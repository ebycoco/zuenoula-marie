<form method="post" action="{{ path('boitesuggestion_delete', {'id': boitesuggestion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ boitesuggestion.id) }}">
    <button class="btn">Delete</button>
</form>
