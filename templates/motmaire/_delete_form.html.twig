<form method="post" action="{{ path('motmaire_delete', {'id': motmaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ motmaire.id) }}">
    <button class="btn">Delete</button>
</form>
