{% extends 'AdminBase1.html.twig' %}


{% block title %}Flash
{% endblock %}

{% block body %}
	<h1>Flash</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ flash.id }}</td>
			</tr>
			<tr>
				<th>Contenue</th>
				<td>{{ flash.contenue }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('admin_flash_index') }}">back to list</a>

	<a href="{{ path('admin_flash_edit', {'id': flash.id}) }}">edit</a>

	{{ include('admin/flash/_delete_form.html.twig') }}

{% endblock %}
