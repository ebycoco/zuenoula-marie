{% extends 'base.html.twig' %}

{% block title %}PresentationMairie
{% endblock %}

{% block body %}
	<h1>PresentationMairie</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ presentation_mairie.id }}</td>
			</tr>
			<tr>
				<th>Contenue</th>
				<td>{{ presentation_mairie.contenue }}</td>
			</tr>
			<tr>
				<th>PublishedAt</th>
				<td>{{ presentation_mairie.publishedAt ? presentation_mairie.publishedAt|date('Y-m-d H:i:s') : '' }}</td>
			</tr>
			<tr>
				<th>UpdatedAt</th>
				<td>{{ presentation_mairie.updatedAt ? presentation_mairie.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('presentation_mairie_index') }}">back to list</a>

	<a href="{{ path('presentation_mairie_edit', {'id': presentation_mairie.id}) }}">edit</a>

	{{ include('presentation_mairie/_delete_form.html.twig') }}
{% endblock %}
