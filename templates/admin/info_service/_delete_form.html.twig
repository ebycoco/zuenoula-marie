<form method="post" action="{{ path('admin_info_service_delete', {'id': info_service.id}) }}" onsubmit="return confirm('être vous sûr de vouloir supprimer cette information?');">

	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ info_service.id) }}">

	<button class="btn btn-danger btn-icon-split">
		<span class="icon text-white-50">
			<i class="fas fa-trash"></i>
		</span>
		<span class="text">Supprimer</span>

	</button>

</form>
