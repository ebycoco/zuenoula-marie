{% extends 'base.html.twig' %}

{% block title %}AdministrationDeMunicipalite{% endblock %}

{% block body %}
    <h1>AdministrationDeMunicipalite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ administration_de_municipalite.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ administration_de_municipalite.titre }}</td>
            </tr>
            <tr>
                <th>Contenue</th>
                <td>{{ administration_de_municipalite.contenue }}</td>
            </tr>
            <tr>
                <th>PublishedAt</th>
                <td>{{ administration_de_municipalite.publishedAt ? administration_de_municipalite.publishedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ administration_de_municipalite.updatedAt ? administration_de_municipalite.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('administration_de_municipalite_index') }}">back to list</a>

    <a href="{{ path('administration_de_municipalite_edit', {'id': administration_de_municipalite.id}) }}">edit</a>

    {{ include('administration_de_municipalite/_delete_form.html.twig') }}
{% endblock %}
