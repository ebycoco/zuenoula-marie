<form method="post" action="{{ path('admin_administration_de_municipalite_delete', {'id': administration_de_municipalite.id}) }}" onsubmit="return confirm('Etre vous sûr de vouloir supprimer le administration_de_municipalite numéro {{ administration_de_municipalite.id }} ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ administration_de_municipalite.id) }}">
	<button class="btn btn-danger btn-icon-split">
		<span class="icon text-white-50">
			<i class="fas fa-trash"></i>
		</span>
		<span class="text">Supprimer</span>
	</button>
</form>
