{% extends 'base.html.twig' %}

{% block title %}Sport{% endblock %}

{% block body %}
    <h1>Sport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sport.id }}</td>
            </tr>
            <tr>
                <th>Contenue</th>
                <td>{{ sport.contenue }}</td>
            </tr>
            <tr>
                <th>PublishedAt</th>
                <td>{{ sport.publishedAt ? sport.publishedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ sport.updatedAt ? sport.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_sport_index') }}">back to list</a>

    <a href="{{ path('admin_sport_edit', {'id': sport.id}) }}">edit</a>

    {{ include('admin_sport/_delete_form.html.twig') }}
{% endblock %}
