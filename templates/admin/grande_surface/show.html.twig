{% extends 'base.html.twig' %}

{% block title %}GrandeSurface{% endblock %}

{% block body %}
    <h1>GrandeSurface</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ grande_surface.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ grande_surface.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ grande_surface.description }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ grande_surface.imageName }}</td>
            </tr>
            <tr>
                <th>PublishedAt</th>
                <td>{{ grande_surface.publishedAt ? grande_surface.publishedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ grande_surface.updatedAt ? grande_surface.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('grande_surface_index') }}">back to list</a>

    <a href="{{ path('grande_surface_edit', {'id': grande_surface.id}) }}">edit</a>

    {{ include('grande_surface/_delete_form.html.twig') }}
{% endblock %}
